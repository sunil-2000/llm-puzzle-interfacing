from enum import Enum


class Selector(Enum):
    START_BUTTON = "pz-moment__button"
    CLOSE_BUTTON = "close-help"
    ITEM_CLASS = "item"
    CORRECT_CLASS = "correct"
    ATTEMPT_CLASS = "bubble"
